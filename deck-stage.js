// deck-stage.js — CONTEUDO ORIGINAL PERDIDO NO UPLOAD
//
// Este arquivo deveria conter a navegação do deck (setas <- ->, grade de
// miniaturas ao apertar "G", impressão em PDF com Ctrl/Cmd+P) e a definição
// do elemento <deck-stage> usado em deck.html.
//
// Durante o "Add files via upload" no GitHub, o conteúdo deste arquivo foi
// trocado com o de deck.html. O conteúdo de deck.html já foi recuperado
// (veja o novo deck.html), mas o conteúdo verdadeiro de deck-stage.js não
// foi encontrado em nenhum outro arquivo do repositório — provavelmente
// ele precisa ser reenviado a partir do site.zip / pasta original no seu
// computador.
//
// Este placeholder evita erros no console enquanto isso; o deck funciona
// e mostra todos os slides, mas sem paginação por JS, miniaturas (G) ou
// impressão (Ctrl/Cmd+P) até esse arquivo ser restaurado.
if (!customElements.get('deck-stage')) {
  customElements.define('deck-stage', class extends HTMLElement {});
}
